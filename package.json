{
  "name": "expressjs12",
  "version": "1.0.0",
  "description": "testfile",
  "main": "npm start",
  "scripts": {
    "start": "nodemon app",
    "start:local": "set NODE_ENV=default && node app",
    "start:dev": "set NODE_ENV=dev && node app",
    "start:stage": "set NODE_ENV=stage && node app",
    "start:prod": "set NODE_ENV=prod && node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ankit",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "config": "^1.30.0",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "mongoose": "^5.0.9",
    "nodemon": "^1.15.1"
  }
}
